hierarchy -top tt_um_danieltyukov_vga_trng -check
  passed: every instantiated module is defined, no blackboxes.
  no blackbox references.

combinational loops reported by 'check':
  none reported
  Every loop reported above is inside ring_osc, which is a ring
  oscillator and is supposed to be a cycle. Loops elsewhere would be
  a bug; grep the raw log for 'ring_osc' to confirm.
  ring_osc mentions in loop log: 47

inferred latch check (any $_DLATCH_ / latch cell below is a bug):
  none: all sequential cells are sg13g2_dfrbpq_1 edge triggered flops
